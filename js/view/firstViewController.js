var FirstViewController = function(view, main, model ) {
	view.createButton.click(function(){
		$(view.container).hide();
        $(selectorView).show();
        $(cartView).show();

	});

	$( "#desc" ).draggable();
    	
}
